spring.application.name=backend

# ===============================
# DATABASE CONFIGURATION
# ===============================
spring.config.import=optional:classpath:application-local.properties

# This driver class is standard for all Postgres connections
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA SETTINGS
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# "update" will automatically create tables in DBeaver when you run the Java app
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# SWAGGER CONFIGURATION
# ===============================
springdoc.swagger-ui.path=/swagger-ui.html

# Database Configuration
spring.datasource.url=jdbc:postgresql://ep-fragrant-term-ag75kvdu-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_HlOW8E2SKTBF